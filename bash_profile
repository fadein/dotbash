# This file is sourced by bash for login shells on the local machine (it is not
# used for SSH and rsh connections). The following line runs your .bashrc and
# is recommended by the bash info pages.

[[ -f ~/.bashrc ]] && source ~/.bashrc

# vim:set foldenable foldmethod=marker filetype=sh tabstop=4 expandtab:
